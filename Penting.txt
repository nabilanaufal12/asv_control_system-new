#Linux
find . -type d -name "__pycache__" -exec rm -r {} +

#Windows
for /d /r . %d in (__pycache__) do @if exist "%d" rd /s /q "%d"

#Linux
pkill -f run_backend_headless.py

#Windows
taskkill /F /IM run_backend_headless.py